# Copy to .env and fill in — .env is gitignored and never committed.
# Get keys: alpaca.markets → Paper Trading → API Keys → Generate
ALPACA_API_KEY=
ALPACA_API_SECRET=
# Live trading requires an explicit "false". Anything else means paper.
ALPACA_PAPER=true
